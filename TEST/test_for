AFC 8 0 //d = 0 
COP 4 8

COP 10 4 // d dans une variable temporaire
AFC 8 11  // 11 dans une variable temporaire
INF CR 10 8 // d<11 résultat mis dans CR
JMF 15 CR //on passe dans le corps du IF en fonction du résultat dans CR

PUSH CR // on save CR en le metant dans la pile
COP 12 4 // on prépare l'incrémentation de d
AFC 14 1 
ADD 12 12 14 // variable temporaire = d+1

AFC 16 13 //corps du for 
COP 2 16

COP 4 12 // on met dans d la valuer de la variable temporaire qui vaut d+1
POP CR // on récupére CR
JMF 2 CR // et on remonte au début du for pour tester la condition

NOP
